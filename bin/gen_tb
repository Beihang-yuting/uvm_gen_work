#!/usr/bin/env python3
"""UVM Testbench Generator entry point."""
import sys
import os

# Add project root to path so it works without pip install
sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), ".."))

from uvm_gen.cli import main

if __name__ == "__main__":
    main()
